{
  "version": "0.1.0",
  "license": "BSD-2-Clause",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "start": "tsdx watch",
    "build": "tsdx build",
    "test": "tsdx test",
    "lint": "tsdx lint",
    "prepare": "tsdx build",
    "size": "size-limit",
    "analyze": "size-limit --why"
  },
  "peerDependencies": {
    "react": ">= 16.8.0"
  },
  "prettier": {
    "bracketSpacing": true,
    "printWidth": 80,
    "semi": false,
    "singleQuote": true,
    "trailingComma": "es5"
  },
  "name": "use-local-credential-storage",
  "description": "React hook for storing and retrieving login credentials for a web app using localStorage",
  "keywords": ["localstorage", "credentials"],
  "author": "Metabolize",
  "module": "dist/use-local-credential-storage.esm.js",
  "size-limit": [
    {
      "path": "dist/use-local-credential-storage.cjs.production.min.js",
      "limit": "10 KB"
    },
    {
      "path": "dist/use-local-credential-storage.esm.js",
      "limit": "10 KB"
    }
  ],
  "devDependencies": {
    "@size-limit/preset-small-lib": "^4.9.1",
    "@types/react": "^17.0.0",
    "react": "^17.0.1",
    "size-limit": "^4.9.1",
    "tsdx": "^0.14.1",
    "tslib": "^2.0.3",
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "local-credential-storage": "^3.1.0"
  }
}
